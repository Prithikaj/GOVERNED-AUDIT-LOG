services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    environment:
      LOCAL_STORAGE: "true"
      PRESIDIO_URL: http://presidio:3000
      DATA_DIR: /data
    ports:
      - "8000:8080"
    volumes:
      - ./.data:/data
